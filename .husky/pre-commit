if ! command -v gitleaks >/dev/null 2>&1; then
  echo "❌ gitleaks not installed. Run: brew install gitleaks" >&2
  echo "   (or skip with --no-verify if you understand the risk)" >&2
  exit 1
fi

gitleaks protect --staged --redact --verbose
