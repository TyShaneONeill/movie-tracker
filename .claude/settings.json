{
  "enabledPlugins": {
    "ralph-loop@claude-plugins-official": true
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "echo '--- Branch check ---' && git branch --show-current && echo '--- TypeScript check ---' && npx tsc --noEmit && echo '--- Unstaged files ---' && git status --short",
            "timeout": 60,
            "statusMessage": "Running pre-commit checks..."
          }
        ]
      }
    ]
  }
}
